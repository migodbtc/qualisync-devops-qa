import { redirect } from "next/navigation";
import React from "react";

export default function TenantsPage() {
  redirect("/wip")

  return (
    <div>
      {/* Tenant Directory content here */}
    </div>
  );
}
